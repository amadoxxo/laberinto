import React from "react";
import ReactDOM from "react-dom/client";
import { LaberintoApp } from "./LaberintoApp";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <LaberintoApp />
  </React.StrictMode>
);
